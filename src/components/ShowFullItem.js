import React from 'react'
import ShowItem from './ShowItem'

export default function ShowFullItem(props) {
  return (
    <div className='full-item'>
        {/* {props.items.map(el =>(
            <ShowItem mainItem={el} key={el.id}/>
        ))} */}
        {}
    </div>
  )
}
